import { Table, Tag, Space, PageHeader } from 'antd';
import Layout from './Layout';

export default function List () {
    return (
      <Layout>
        <PageHeader />
        
      </Layout>
    )
}